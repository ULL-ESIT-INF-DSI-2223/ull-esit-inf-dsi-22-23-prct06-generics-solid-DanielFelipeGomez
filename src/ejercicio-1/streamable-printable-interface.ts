/**
 * si se desea implementar esta interfaz, 
 * se deberá tener un método *print* que 
 * retorna una cadena con formato
 */
export interface StreamablePrintable {
    /**
     * Se pretende definir un método que retorne una cadena con 
     * formato para ser impresa
     */
    print(): string;
}
